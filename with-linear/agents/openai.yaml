interface:
  display_name: "With Linear"
  short_description: "Work from a Linear issue and keep it updated."
  default_prompt: "Use $with-linear to work from this Linear issue and maintain one concise current summary at confirmed checkpoints; keep exploratory discussion in chat."

policy:
  allow_implicit_invocation: true
